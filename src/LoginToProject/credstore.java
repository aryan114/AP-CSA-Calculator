/*
package LoginToProject;



import view_control.MainMenu;

import javax.swing.*;
import java.util.HashMap;

public class credstore {
//Pranav - Model attempt
    private HashMap<String, String> creds = new HashMap<String, String>();

    public credstore(){
        HashMap<String, String> creds = new HashMap<String, String>();
        this.creds = creds;
    }

    public void adder(String x, String y){
        creds.put(x, y);
    }

    public void checker (JTextField x, JPasswordField y){
        if (creds.get(x).equals(y)){
            boolean Login = true;
            MainMenu.main(null);
        } else{
            boolean Login = false;
        }
    }

    public HashMap<String, String> getCreds() {
        return creds;
    }
}

*/
